# DevMind 检索评估报告

## 结论

- 评估问题：20 条；
- 命中：18 条；
- 命中率：90%；
- 环境：mock 模型 + 知识库「用户1知识库」的 `MySQL索引专题.md`。

## 未命中的问题

| 问题 | 期望关键词 | 原因 |
| --- | --- | --- |
| type=ALL 是什么问题 | 全表扫描 | 当前文档没有出现“全表扫描”原文 |
| Using temporary 怎么优化 | 临时表 | 当前文档没有出现“临时表”原文 |

这两条不是检索算法错误，而是知识库缺少对应资料。补充相关文档后命中率会继续提升。

## 评估方式

1. 每个问题先做向量 + 关键词混合检索，召回 10 条；
2. 再经过 Rerank 取前 5 条；
3. 前 5 条中只要有一条包含期望关键词，就记为命中；
4. 接口：`POST /api/evaluations/retrieval`。

## 完整结果

| 问题 | 期望关键词 | 命中 |
| --- | --- | --- |
| MySQL 深分页为什么会慢 | 深分页 | 是 |
| 深分页怎么优化 | 深分页 | 是 |
| 延迟关联是什么 | 延迟关联 | 是 |
| 索引失效有哪些场景 | 索引失效 | 是 |
| 左模糊查询为什么慢 | 左模糊 | 是 |
| 对索引列使用函数会怎样 | 索引 | 是 |
| 隐式类型转换导致什么问题 | 索引 | 是 |
| 最左前缀原则是什么意思 | 最左前缀 | 是 |
| 联合索引怎么用 | 联合索引 | 是 |
| EXPLAIN 看哪些字段 | 执行计划 | 是 |
| type=ALL 是什么问题 | 全表扫描 | 否 |
| Using filesort 怎么解决 | filesort | 是 |
| Using temporary 怎么优化 | 临时表 | 否 |
| 覆盖索引有什么好处 | 覆盖索引 | 是 |
| order by 为什么慢 | 索引 | 是 |
| group by 为什么慢 | 索引 | 是 |
| join 太慢怎么排查 | 执行计划 | 是 |
| 慢查询怎么定位 | 执行计划 | 是 |
| 索引基数是什么 | 索引 | 是 |
| count 大表怎么优化 | 索引 | 是 |
