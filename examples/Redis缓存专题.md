# Redis 缓存淘汰策略

Redis 常见的内存淘汰策略包括 LRU、LFU 和随机淘汰。

## LRU 近似实现

Redis 的 LRU 是近似 LRU，通过采样来近似最近最少使用顺序。

## 缓存穿透与击穿

缓存穿透是指查询不存在的数据，缓存击穿是指热点 key 过期瞬间大量请求打到数据库。

## 缓存雪崩

缓存雪崩是指大量 key 同时过期，导致数据库压力骤增，可以通过过期时间加随机抖动缓解。

## 布隆过滤器

布隆过滤器可以用来拦截不存在的 key，减少缓存穿透。
