# DevMind Prometheus 抓取配置
global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules.yml

scrape_configs:
  - job_name: devmind-app
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ['devmind-app:8080', 'devmind-app2:8081']
